# คู่มือการติดตั้งระบบหลังบ้าน (Backend Setup Guide) 🚀🌳

ทำตามขั้นตอนด้านล่างนี้เพื่อนำระบบของคุณขึ้นใช้งานบน Host จริงครับ

## 1. การเตรียมตัว
- ตรวจสอบว่า Host ของคุณรองรับ **PHP (เวอร์ชัน 7.4 ขึ้นไป)** และมี **MySQL/MariaDB**
- เตรียมข้อมูล Database จากแผงควบคุม (cPanel / DirectAdmin / Plesk) ของคุณ:
  - **Database Host**: ส่วนใหญ่เป็น `localhost`
  - **Database Name**: ชื่อฐานข้อมูลที่คุณสร้าง
  - **Username**: ชื่อผู้ใช้ฐานข้อมูล
  - **Password**: รหัสผ่านของผู้ใช้ฐานข้อมูล

## 2. อัปโหลดไฟล์
1. นำโฟลเดอร์ `api/` และไฟล์ `schema.sql` ขึ้นไปไว้บน Host (ผ่าน FTP หรือ File Manager)
2. **สำคัญ**: ตรวจสอบให้มั่นใจว่าโครงสร้างไฟล์เป็นดังนี้:
   ```
   /public_html
     /api
       api.php
       install.php
       config.php (จะถูกสร้างภายหลัง)
     schema.sql
   ```

## 3. รันตัวติดตั้งอัตโนมัติ (Visual Installer)
1. เปิด Web Browser แล้วพิมพ์ URL: `https://yourdomain.com/api/install.php`
2. คุณจะเห็นหน้าจอติดตั้งสีเขียว 🌳
3. กรอกข้อมูล Database ที่เตรียมไว้ในข้อ 1
4. กด **"เชื่อมต่อและติดตั้ง"**
5. หากสำเร็จ ระบบจะแสดงเครื่องหมายติ๊กถูก ✅ และสร้างไฟล์ `config.php` ให้คุณอัตโนมัติ

## 4. ตรวจสอบความปลอดภัย
> [!CAUTION]
> หลังจากติดตั้งเสร็จแล้ว **ให้ลบไฟล์ `api/install.php` ทิ้งทันที** เพื่อป้องกันไม่ให้คนอื่นมาเปลี่ยนรหัสฐานข้อมูลของคุณ

## 5. เชื่อมต่อระบบหน้าบ้าน (React)
ขั้นตอนสุดท้ายหลังจากได้ Backend แล้ว:
1. เปิดไฟล์ `LoanCalculator.js` ในโปรเจค React ของคุณ
2. มองหาบรรทัดที่เขียนว่า `const API_BASE_URL`
3. เปลี่ยนให้เป็น URL จริงของไฟล์ `api.php` บน Host:
   ```javascript
   const API_BASE_URL = 'https://yourdomain.com/api/api.php';
   ```
4. Build และ Deploy หน้าบ้านไปยัง GitHub Pages หรือ Host ที่คุณต้องการ

---
> [!TIP]
> หากติดปัญหาเรื่อง **CORS** (หน้าบ้านเชื่อมต่อหลังบ้านไม่ได้) ให้ตรวจสอบใน `api/config.php` ว่ามีบรรทัด `Access-Control-Allow-Origin: *` หรือไม่ หากต้องการความปลอดภัยสูงขึ้น ให้เปลี่ยน `*` เป็น URL ของเว็บหน้าบ้านคุณครับ
